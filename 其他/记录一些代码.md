```
collapse
import { Collapse as AntdCollapse } from "antd";
import { ReactComponent as Packup } from "./icons/icon-unfold.svg";
import styled from "styled-components";
import React, { ReactElement } from "react";
const { Panel } = AntdCollapse;

const Contain = styled.div`
  background: #f2f7fc;
  //每一项的padding 每一个展开项 这里可以设置margin
  .ant-collapse > .ant-collapse-item > .ant-collapse-header {
    padding: 0;
    height: 25px;
    margin-left: 2px;
    font-family: PingFangSC-Medium;
    font-size: 13px;
    color: #333333;
    line-height: 25px;
    user-select: none;
  }
  //每一项的内容的边距 字体
  .ant-collapse-ghost
    > .ant-collapse-item
    > .ant-collapse-content
    > .ant-collapse-content-box {
    padding: 0;
    font-family: PingFangSC-Medium;
    font-size: 13px;
    line-height: 13px;
    margin-left: 14px;
    user-select: none;
  }
  //图标的边距
  .ant-collapse
    > .ant-collapse-item
    > .ant-collapse-header
    .ant-collapse-arrow {
    margin-right: 0;
  }
`;
const PackupIcon = styled(Packup)<{ deg?: number }>`
  height: 12px;
  width: 12px;
  margin-right: 2px;
  transform: rotate(${(props) => (props.deg ? props.deg : 0)}deg);
`;
interface IpanelConfig {
  title: string;
  data: JSX.Element | ReactElement | string;
  key: number | string;
}
interface Iprops {
  Config: Array<IpanelConfig>;
  activeKey: string | string[];
  changeKey: (config: string | string[]) => void;
}
export const Collapse = (props: Iprops) => {
  const { Config, activeKey, changeKey } = props;
  const handlechange = (e: string | string[]) => {
    console.log(e);
    changeKey && changeKey(e);
  };
  return (
    <Contain>
      <AntdCollapse
        activeKey={activeKey}
        ghost
        expandIcon={({ isActive }) =>
          isActive ? <PackupIcon /> : <PackupIcon deg={-90} />
        }
        onChange={handlechange}
      >
        {Config && Config.length > 0
          ? Config.map((item, index) => {
              return (
                <React.Fragment key={index}>
                  <Panel header={item.title} key={item.key} showArrow={true}>
                    {item.data}
                  </Panel>
                </React.Fragment>
              );
            })
          : null}
      </AntdCollapse>
    </Contain>
  );
};
import { useState } from "react";
import { CompName, Section } from ".";
import { Collapse } from "./Collapase";

const Content = () => {
  const [key, setkey] = useState<string | string[]>([]);
  const handlechange = (config: string | string[]) => {
    setkey(config);
  };
  return (
    <Collapse
      activeKey={key}
      changeKey={handlechange}
      Config={[
        { title: "first", data: "data1", key: 1 },
        { title: "Second", data: "data2", key: 2 },
        { title: "Third", data: "data3", key: 3 },
      ]}
    />
  );
};
export const LeftContent = () => {
  const [key, setkey] = useState<string | string[]>([]);
  const handlechange = (config: string | string[]) => {
    setkey(config);
  };
  return (
    <>
      <CompName compname="Page 01" width={288} />
      <Section name="搜索需求与变量" width={288}>
        <Collapse
          activeKey={key}
          changeKey={handlechange}
          Config={[
            { title: "first", data: <Content />, key: 1 },
            { title: "Second", data: "data2", key: 2 },
            { title: "Third", data: "data3", key: 3 },
          ]}
        />
      </Section>
    </>
  );
};

```



```
import { useState } from "react";
import styled from "styled-components";
import "./icon.css";
import { Menu, Dropdown } from "antd";
//antd 下拉菜单
const menu = (
  <Menu>
    <Menu.Item key="0">
      <a href="https://www.antgroup.com">1st menu item</a>
    </Menu.Item>
    <Menu.Item key="1">
      <a href="https://www.aliyun.com">2nd menu item</a>
    </Menu.Item>
    <Menu.Divider />
    <Menu.Item key="3">3rd menu item</Menu.Item>
  </Menu>
);

//省去了每一个图标都要有一个class 所有的图标都要继承自这一个样式
const Iconstyles = styled.i`
  font-size: 16px;
  font-family: "iconfont" !important;
  font-style: normal;
  -webkit-font-smoothing: antialiased;
  -webkit-text-stroke-width: 0.2px;
  -moz-osx-font-smoothing: grayscale;
`;
/**
 * 头部 两个主要内容的部分 大框
 */
const RightPanelHeader = styled.div`
  width: 312px;
  height: 40px;
  background: #f5f5f6;
`;
//每个文本和字体的小框
const RightPanelHeaderItem = styled.div`
  display: inline-block;
  cursor: pointer;
`;
//文字下划线
const SolidTextUnderLine: any = styled.div`
  height: 0;
  border: 1px solid #222222;
  width: 48px;
  border-radius: 1px;
  background-color: #222222;
  margin-left: ${(props: any) => props.width};
  display: ${(props: any) => props.show};
`;
//头部的文本 主要有两个
const RightPanelHeaderTextAttribute = styled.span`
  font-family: PingFangSC-Medium;
  font-size: 14px;
  color: ${(props) => props.color || "#222222"};
  line-height: 40px;
  margin-left: 5px;
`;
const RightPanelHeaderTextInsert = styled.span`
  font-family: PingFangSC-Medium;
  font-size: 14px;
  line-height: 40px;
  margin-left: 4px;
  color: ${(props) => props.color || "#222222"};
`;
//头部的小图标
const RightPanelHeaderIattribute = styled(Iconstyles)`
  font-size: 14px;
  margin-left: 17px;
  color: ${(props) => props.color};
`;
const RightPanelHeaderIinsert = styled(Iconstyles)`
  font-size: 14px;
  margin-left: 24px;
  color: ${(props) => props.color};
`;
//顶部的栏 属性+插入 大组件 集成了小组件在里面的
const RightPanelHeaderFn = (props: any) => {
  const { change } = props;
  const [iconcolor, seticoncolor] = useState({
    color: "##222222",
    othercolor: "#989db3",
  });
  const showAttribute = () => {
    change && change(1);
    seticoncolor({
      color: "#222222",
      othercolor: "#989db3",
    });
    setshowUnderline(1);
  };
  const showInsert = () => {
    change && change(0);
    seticoncolor({
      color: "#989db3",
      othercolor: "#222222",
    });
    setshowUnderline(0);
  };
  const [showUnderline, setshowUnderline] = useState(1);
  return (
    <RightPanelHeader>
      <RightPanelHeaderItem onClick={showAttribute}>
        <RightPanelHeaderIattribute color={iconcolor.color}>
          &#xe63a;
        </RightPanelHeaderIattribute>
        <RightPanelHeaderTextAttribute color={iconcolor.color}>
          属性
        </RightPanelHeaderTextAttribute>
        <SolidTextUnderLine
          width={"16px"}
          show={showUnderline ? "block" : "none"}
        ></SolidTextUnderLine>
      </RightPanelHeaderItem>
      <RightPanelHeaderItem onClick={showInsert}>
        <RightPanelHeaderIinsert color={iconcolor.othercolor}>
          &#xe642;
        </RightPanelHeaderIinsert>
        <RightPanelHeaderTextInsert color={iconcolor.othercolor}>
          插入
        </RightPanelHeaderTextInsert>
        <SolidTextUnderLine
          width={"23px"}
          show={showUnderline ? "none" : "block"}
        ></SolidTextUnderLine>
      </RightPanelHeaderItem>
    </RightPanelHeader>
  );
};
/**
 * 右侧组件名字位置
 */
const RightPanelCompName = styled.div`
  width: 312px;
  height: 46px;
  border-bottom: 1px solid #e1e3eb;
`;
const RightCompNameText = styled.span`
  font-family: PingFangSC-Medium;
  font-size: 16px;
  color: #222222;
  line-height: 46px;
  margin-left: 16px;
`;
const RightPanelCompNameicon = styled(Iconstyles)`
  font-size: 16px;
  color: #8b8fa3;
  float: right;
  margin-right: 16px;
  height: 20px;
  width: 20px;
  margin-top: 13px;
  margin-bottom: 13px;
`;
//组件名字的位置接口+组件
interface RightCompNameFnIprops {
  compname: string;
}
const RightCompNameFn = (props: RightCompNameFnIprops) => {
  return (
    <RightPanelCompName>
      <RightCompNameText>{props.compname}</RightCompNameText>
      <RightPanelCompNameicon>&#xe640;</RightPanelCompNameicon>
    </RightPanelCompName>
  );
};

/**
 * 右侧折叠面板 *
 */
const RightFlodPanelItem = styled.div`
  width: 312px;
  border-bottom: 1px solid #e1e3eb;
`;
//控制面板的div
const RightFlodPanellabel = styled.div`
  height: 46px;
`;
//控制面板上半部分的文字 以及图标 旋转180度
const RightFlodPanelSpan = styled.span`
  font-family: PingFangSC-Medium;
  font-size: 14px;
  color: #8b8fa3;
  line-height: 46px;
  margin-left: 16px;
`;
const RightFlodPaneli = styled(Iconstyles)`
  font-size: 18px;
  height: 20px;
  width: 20px;
  float: right;
  margin-top: 13px;
  margin-bottom: 13px;
  margin-right: 16px;
  color: #8b8fa3;
  transform: rotate(180deg);
`;
//下划线 虚线
const DashUnderline: any = styled.div`
  height: 0;
  border-bottom: 1px dashed #a1a5b3;
  width: ${(props: any) => props.width};
  margin-left: ${(props: any) => (props.left ? props.left : "16px")};
  margin-top: 1px;
`;
/**
 * 主要内容区域
 */
const RightFlodPanelContent = styled.div``;
//内容区域的文字 数据 空态 压缩模式
const RightFlodPanelContentp: any = styled.p`
  font-family: PingFangSC-Regular;
  font-size: 13px;
  color: #222222;
  line-height: 13px;
  margin-left: 16px;
  margin-bottom: 0;
  margin-top: 0;
  display: ${(props: any) => (props.display ? props.display : "block")};
`;
//输入框
const RightFlodPanelContentInput = styled.input`
  width: 280px;
  height: 32px;
  background: #fdfdfd;
  border: 1.07px solid #d7d9e0;
  border-radius: 6px;
  margin-left: 16px;
  margin-top: 6px;
  margin-bottom: 12px;
  font-size: 13px;
  padding-left: 12px;
`;
//switch开关
const RightFlodPanelContentSwitch = styled.input`
  width: 24px;
  height: 14px;
  position: relative;
  background-color: #dedede;
  border-radius: 7px;
  background-clip: content-box;
  display: inline-block;
  -webkit-appearance: none;
  user-select: none;
  outline: none;
  margin-left: 16px;
  top: 50%;
  transform: translateY(23%);
  :before {
    content: "";
    width: 10px;
    height: 10px;
    position: absolute;
    margin-top: 2px;
    margin-left: 2px;
    border-radius: 20px;
    background-color: #ffffff;
    transition: left 0.3s;
  }
  :checked {
    border-color: #636775;
    background-color: #636775;
  }
  :checked:before {
    left: 10px;
    transition: left 0.3s;
  }
`;
//switch开关的div
const RightFlodPanelContentSwitchDiv = styled.div`
  margin-bottom: 11px;
`;
//switch开关的文字
const RightFlodPanelContentSwitchText = styled.span`
  display: inline-block;
  font-family: PingFangSC-Regular;
  font-size: 13px;
  color: #222222;
  line-height: 13px;
  margin-bottom: 0;
  margin-left: 8px;
`;
//表格的信息 列数
const TableColumnNumbers = styled.span`
  font-family: PingFangSC-Regular;
  font-size: 13px;
  color: #8b8fa3;
  line-height: 13px;
  margin-left: 4px;
`;
// 增加列的文字
const AddColumns = styled.span`
  font-family: PingFangSC-Regular;
  font-size: 12px;
  color: #4965f2;
  text-align: right;
  line-height: 12px;
  float: right;
  margin-right: 16px;
`;
//折叠面板的被折叠部分 基础组件
const RightFlodPanelBasic = () => {
  return (
    <RightFlodPanelContent>
      <RightFlodPanelContentp>数据</RightFlodPanelContentp>
      <DashUnderline width={"25px"}></DashUnderline>
      <RightFlodPanelContentInput></RightFlodPanelContentInput>
      <RightFlodPanelContentp>空态</RightFlodPanelContentp>
      <DashUnderline width={"25px"}></DashUnderline>
      <RightFlodPanelContentInput></RightFlodPanelContentInput>
      <RightFlodPanelContentp>压缩模式</RightFlodPanelContentp>
      <RightFlodPanelContentInput></RightFlodPanelContentInput>
      <RightFlodPanelContentSwitchDiv>
        <RightFlodPanelContentSwitch type="checkbox"></RightFlodPanelContentSwitch>
        <RightFlodPanelContentSwitchText>
          显示筛选器
        </RightFlodPanelContentSwitchText>
        <DashUnderline width={"64px"} left={"48px"}></DashUnderline>
      </RightFlodPanelContentSwitchDiv>
      <RightFlodPanelContentSwitchDiv>
        <RightFlodPanelContentSwitch type="checkbox"></RightFlodPanelContentSwitch>
        <RightFlodPanelContentSwitchText>
          显示新建选项
        </RightFlodPanelContentSwitchText>
        <DashUnderline width={"77px"} left={"48px"}></DashUnderline>
      </RightFlodPanelContentSwitchDiv>
      <RightFlodPanelContentSwitchDiv>
        <RightFlodPanelContentSwitch type="checkbox"></RightFlodPanelContentSwitch>
        <RightFlodPanelContentSwitchText>
          显示下载按钮
        </RightFlodPanelContentSwitchText>
      </RightFlodPanelContentSwitchDiv>
      <RightFlodPanelContentSwitchDiv>
        <RightFlodPanelContentSwitch type="checkbox"></RightFlodPanelContentSwitch>
        <RightFlodPanelContentSwitchText>
          显示加载状态
        </RightFlodPanelContentSwitchText>
      </RightFlodPanelContentSwitchDiv>
    </RightFlodPanelContent>
  );
};
//表格内的settingsDiv
const TableSettingsDiv = styled.div`
  width: 280px;
  border: 1px solid #d7d9e0;
  border-radius: 6px;
  margin-left: 16px;
  margin-top: 5px;
  /* box-sizing: content-box; */
  overflow: hidden;
`;
//表格内的settings每一行
const TableSettingRow = styled.div`
  color: #ffffff;
  width: 280px;
  height: 32px;
  border-bottom: 1px solid #d7d9e0;
  :hover {
    background-color: #d7d9e0;
  }
  :hover > input {
    background-color: #d7d9e0;
  }
  :last-child {
    border-bottom: none;
  }
`;
const TableSettingRowTag = styled.span`
  font-family: PingFangSC-Medium;
  font-size: 13px;
  color: #989db3;
  line-height: 13px;
  margin-top: 9.5px;
`;
const TableSettingRowInput = styled.input`
  font-family: PingFangSC-Medium;
  font-size: 13px;
  height: 13px;
  width: 174px;
  line-height: 13px;
  border: none;
  margin-top: 9.5px;
  margin-left: 34px;
  color: #333333;
`;
//小图标 小眼睛
const TableSettingRowIconEyes = styled(Iconstyles)`
  font-size: 16px;
  margin-left: 17px;
  color: #8b8fa3;
`;
//表格内的小型下拉框
const MyDropdown = styled(Dropdown)`
  display: inline-block;
  height: 32px !important;
  width: 112px !important;
  background: #fdfdfd;
  border: 1.07px solid #d7d9e0;
  border-radius: 6px;
`;
//折叠面板的被折叠部分 表格组件
const RightPanelForm = () => {
  return (
    <RightFlodPanelContent>
      <>
        <RightFlodPanelContentp display={"inline-block"}>
          列数
        </RightFlodPanelContentp>
        <TableColumnNumbers>({"23"})</TableColumnNumbers>
        <AddColumns>增加列</AddColumns>
      </>
      <TableSettingsDiv>
        <TableSettingRow>
          <TableSettingRowTag>标题</TableSettingRowTag>
          <TableSettingRowTag>可编辑</TableSettingRowTag>
        </TableSettingRow>
        <TableSettingRow>
          <TableSettingRowInput placeholder={"123"}></TableSettingRowInput>
          <TableSettingRowIconEyes>&#xe63e;</TableSettingRowIconEyes>
          <TableSettingRowIconEyes>&#xe63d;</TableSettingRowIconEyes>
        </TableSettingRow>
        <TableSettingRow>
          <TableSettingRowInput placeholder={"123"}></TableSettingRowInput>
        </TableSettingRow>
        <TableSettingRow>
          <TableSettingRowInput placeholder={"123"}></TableSettingRowInput>
        </TableSettingRow>
        <TableSettingRow>
          <TableSettingRowInput placeholder={"123"}></TableSettingRowInput>
        </TableSettingRow>
        <TableSettingRow>
          <TableSettingRowInput placeholder={"123"}></TableSettingRowInput>
        </TableSettingRow>
      </TableSettingsDiv>
      <RightFlodPanelContentp>表格排序</RightFlodPanelContentp>
      <MyDropdown overlay={menu} trigger={["click"]}>
        <a onClick={(e) => e.preventDefault()}>
          Click me<TableSettingRowIconEyes>&#xe63d;</TableSettingRowIconEyes>
        </a>
      </MyDropdown>
      <RightFlodPanelContentSwitchDiv>
        <RightFlodPanelContentSwitch type="checkbox"></RightFlodPanelContentSwitch>
        <RightFlodPanelContentSwitchText>
          使用表格设置
        </RightFlodPanelContentSwitchText>
      </RightFlodPanelContentSwitchDiv>
    </RightFlodPanelContent>
  );
};
//控制内容是否展示
const RightFlodPanelShow: any = styled.div`
  display: ${(props: any) => props.show || "none"};
`;
// 右侧折叠面板主组件 整合了小组件
const RightFlodPanelFn = (props: any) => {
  const [showditems, setshowitems] = useState<number>(1);
  const handlechangeshow = () => {
    showditems ? setshowitems(0) : setshowitems(1);
  };
  return (
    <RightFlodPanelItem>
      <RightFlodPanellabel onClick={handlechangeshow}>
        <RightFlodPanelSpan>{props.flodname}</RightFlodPanelSpan>
        <RightFlodPaneli>&#xe643;</RightFlodPaneli>
      </RightFlodPanellabel>
      <RightFlodPanelShow show={showditems ? "block" : "none"}>
        {props.children}
      </RightFlodPanelShow>
    </RightFlodPanelItem>
  );
};

/**
 * 总函数 返回右面的全部内容
 */
// 属性/插入的大框 使用display控制是否展示 避免多次渲染虚拟Dom
const RighePanelAttributeDiv: any = styled.div`
  display: ${(props: any) => props.show};
`;
const RightPanelInsertDiv: any = styled.div`
  display: ${(props: any) => props.show};
`;

export const RightPanelContainer = () => {
  const [showContent, setshowContent] = useState<number>(1);
  const handlechange = (status: number) => {
    setshowContent(status);
  };
  return (
    <>
      <RightPanelHeaderFn change={handlechange}></RightPanelHeaderFn>
      <RighePanelAttributeDiv show={showContent ? "block" : "none"}>
        <RightCompNameFn compname="Table1"></RightCompNameFn>
        <RightFlodPanelFn flodname="基础">
          <RightFlodPanelBasic></RightFlodPanelBasic>
        </RightFlodPanelFn>
        <RightFlodPanelFn flodname="表格">
          <RightPanelForm></RightPanelForm>
        </RightFlodPanelFn>
      </RighePanelAttributeDiv>
      <RightPanelInsertDiv show={showContent ? "none" : "block"}>
        <h1>Insert</h1>
      </RightPanelInsertDiv>
    </>
  );
};

```

```
import React from "react";
import styled from "styled-components";
import { Link } from "react-router-dom";
import { ReactComponent as LogoIcon } from "./icons/logo.svg";
import { ReactComponent as MoreCodeIcon } from "./icons/more-code.svg";
import { ReactComponent as DownIcon } from "./icons/down.svg";

import { Button, Collapse, Tree } from "antd";
import { DownOutlined } from "@ant-design/icons";
const { Panel } = Collapse;

const HeaderWrapper = styled.header`
  display: flex;
  width: 100%;
  height: 54px;
  background-color: #181818;
  filter: drop-shadow(0px 1px 0px #ebebeb);
  padding: 8px 16px;
  justify-content: space-between;
`;

export const Height100Div = styled.div`
  height: 100%;
`;
const HeaderStart = styled.div`
  display: flex;
  align-items: center;
`;
const HeaderMiddle = styled.div`
  display: flex;
  align-items: center;
`;
const HeaderEnd = styled.div`
  display: flex;
  align-items: center;
`;
const HeaderFont = styled.div`
  font-family: Roboto;
  font-style: normal;
  font-weight: normal;
  font-size: 14px;
  line-height: 24px;
  color: #ffffff;
  margin-right: 8px;
  margin-left: 10px;
`;
export const Body = styled.div`
  height: calc(100vh - 54px);
  background: #f1f2f5;
  padding: 10px 8px;
  display: flex;
`;
const CardHead = styled.div`
  height: 38px;
  margin: 0px 20px;
  border-bottom: 1px solid rgba(0, 0, 0, 0.03);
  padding: 20px 0px;
`;

//<CardHead/>
const Card = ({ className, children }: any) => (
  <div className={className}>{children}</div>
);

const StyledCard = styled(Card)`
  width: 280px;
  height: 100%;
  background: #ffffff;
  border: 1px solid #f0f0f0;
  box-sizing: border-box;
  border-radius: 2px;
  overflow: hidden;
`;
const RightStyledCard = styled(Card)`
  width: 312px;
  height: 100%;
  background: #ffffff;
  box-sizing: border-box;
  overflow: auto;
  border-left: 1px solid #d7d9e0;
`;
const text = `
  A dog is a type of domesticated animal.
  Known for its loyalty and faithfulness,
  it can be found as a welcome guest in many households across the world.
`;
const treeData = [
  {
    title: "parent 1-0",
    key: "0-0-0",
    children: [
      {
        title: "leaf",
        key: "0-0-0-0",
      },
      {
        title: "leaf",
        key: "0-0-0-1",
      },
      {
        title: "leaf",
        key: "0-0-0-2",
      },
    ],
  },
  {
    title: "parent 1-1",
    key: "0-0-1",
    children: [
      {
        title: "leaf",
        key: "0-0-1-0",
      },
    ],
  },
  {
    title: "parent 1-2",
    key: "0-0-2",
    children: [
      {
        title: "leaf",
        key: "0-0-2-0",
      },
      {
        title: "leaf",
        key: "0-0-2-1",
      },
    ],
  },
];
export const LeftPanel2 = () => (
  <StyledCard>
    <Collapse defaultActiveKey={["1", "2", "3"]}>
      <Panel header="组件列表" key="1">
        <Tree
          switcherIcon={<DownOutlined />}
          defaultExpandedKeys={["0-0-0"]}
          treeData={treeData}
        />
      </Panel>
      <Panel header="查询列表" key="2">
        <p>{text}</p>
      </Panel>
    </Collapse>
  </StyledCard>
);
export const LeftPanel = styled(StyledCard)``;
export const RightPanel = styled(RightStyledCard)``;
export const EditorContainer = styled.div`
  height: 100%;
  background: #ffffff;
  border: 1px solid #f0f0f0;
  box-sizing: border-box;
  border-radius: 2px;
  flex: 1 0 0;
  margin: 0 10px;
  overflow: auto;
`;
export const StyledLink = styled.a`
  display: flex;
  align-items: center;
`;
//align-items: "center";

export function Header() {
  return (
    <HeaderWrapper>
      <HeaderStart>
        <StyledLink>
          <LogoIcon />
          <MoreCodeIcon />
        </StyledLink>
        <HeaderFont>My First Application</HeaderFont>
        <DownIcon />
      </HeaderStart>
      <HeaderMiddle>
        <HeaderFont>middle</HeaderFont>
      </HeaderMiddle>
      <HeaderEnd>
        <HeaderFont>end</HeaderFont>
      </HeaderEnd>
    </HeaderWrapper>
  );
}

```

```
const RightPanelHeader = styled.div`
  width: 312px;
  height: 40px;
  background: #f5f5f6;
`;
//每个文本和字体的小框
const RightPanelHeaderItem = styled.div`
  display: inline-block;
  cursor: pointer;
`;

//input框
const UnderlineInput = styled.input`
  /* display: none; */
  :checked ~ .qpp {
    border-bottom: 1px solid #ccc;
  }
  :checked ~ .label2 {
    border-bottom: 1px solid#222222;
  }
  position: relative;
`;
const UnderlineLabel = styled.label``;
//头部的文本 主要有两个
const RightPanelHeaderTextAttribute = styled.span`
  font-family: PingFangSC-Medium;
  font-size: 14px;
  color: ${(props) => props.color || "#222222"};
  line-height: 40px;
  margin-left: 5px;
  position: relative;
  :checked:before {
    content: "";
    position: absolute;
    width: 48px;
    left: -19px;
    right: 0;
    bottom: -11px;
    height: 0;
    border: 1px solid#222222;
    border-radius: 1px;
  }
`;
const RightPanelHeaderTextInsert = styled.span`
  font-family: PingFangSC-Medium;
  font-size: 14px;
  line-height: 40px;
  margin-left: 4px;
  color: ${(props) => props.color || "#222222"};
`;
//头部的小图标
const RightPanelHeaderIattribute = styled(Iconstyles)`
  font-size: 14px;
  margin-left: 17px;
  color: ${(props) => props.color};
`;
const RightPanelHeaderIinsert = styled(Iconstyles)`
  font-size: 14px;
  margin-left: 24px;
  color: ${(props) => props.color};
`;
//顶部的栏 属性+插入 大组件 集成了小组件在里面的
const RightPanelHeaderFn = (props: any) => {
  const { change } = props;
  const showAttribute = () => {
    change && change(1);
  };
  const showInsert = () => {
    change && change(0);
  };
  return (
    <RightPanelHeader>
      <UnderlineInput type="checkbox" id="input1"></UnderlineInput>
      <UnderlineLabel 
      htmlFor="input1" className="label1">
        <span className="qpp"></span>
        <RightPanelHeaderItem onClick={showAttribute}>
          <RightPanelHeaderIattribute>&#xe63a;</RightPanelHeaderIattribute>
          <RightPanelHeaderTextAttribute>属性</RightPanelHeaderTextAttribute>
        </RightPanelHeaderItem>
      </UnderlineLabel>
      <UnderlineLabel htmlFor="input1" className="label2">
        <RightPanelHeaderItem onClick={showInsert}>
          <RightPanelHeaderIinsert>&#xe642;</RightPanelHeaderIinsert>
          <RightPanelHeaderTextInsert>插入</RightPanelHeaderTextInsert>
        </RightPanelHeaderItem>
      </UnderlineLabel>
    </RightPanelHeader>
  );
};
```



```
/**
 * 
 *       <ToolTip
        title={<span style={{ color: "#000" }}>{tooltip}</span>}
        color="white"
        arrowPointAtCenter={true}
        placement="top"
        defaultVisible={false}
        trigger="hover"
      >
        <RightFlodPanelContentp border={DashUnderline}>
          {label}
        </RightFlodPanelContentp>
      </ToolTip>
 * //antd 气泡卡片
const content = (
  <div>
    <p>Content</p>
    <p>Content</p>
  </div>
);
 *       <Popover content={content} title="Title" placement="left"></Popover>
 *       <Popover content={content} title="Title" placement="left">
        <RightFlodPanelContentp border={DashUnderline}>
          空态
        </RightFlodPanelContentp>
      </Popover>
      <RightFlodPanelContentInput></RightFlodPanelContentInput>
      <RightFlodPanelContentp>压缩模式</RightFlodPanelContentp>
      <RightFlodPanelContentInput
        Inputbottom={"10.5px"}
      ></RightFlodPanelContentInput>
 */

```



```
/**
 * //折叠面板的被折叠部分 基础组件
const RightFlodPanelBasic = () => {
  return (
    <RightFlodPanelContent>
      <InputStyle label="数据" tooltip="这是文本提示" value=""></InputStyle>
      <InputStyle label="空态" tooltip="这是文本提示" value=""></InputStyle>
      <InputStyle
        label="压缩模式"
        tooltip=""
        value=""
        marginBottom="10.5px"
      ></InputStyle>
      <SwitchStyle
        label="显示筛选器"
        tooltip="这是一个Switch"
        value=""
      ></SwitchStyle>
      <SwitchStyle
        label="显示新建按钮"
        tooltip="这是一个Switch"
        value=""
      ></SwitchStyle>
      <SwitchStyle label="显示下载按钮" tooltip="" value=""></SwitchStyle>
      <SwitchStyle label="显示加载状态" tooltip="" value=""></SwitchStyle>
    </RightFlodPanelContent>
  );
};

 */

```



```
tableSwitch
//switch开关
// const Switch = styled.input`
//   width: 24px;
//   height: 14px;
//   position: relative;
//   background-color: #dedede;
//   border-radius: 7px;
//   background-clip: content-box;
//   display: inline-block;
//   -webkit-appearance: none;
//   user-select: none;
//   outline: none;
//   margin-left: 16px;
//   top: 50%;
//   transform: translateY(23%);
//   transition: all 0.4s ease;
//   :hover {
//     cursor: pointer;
//   }
//   :checked {
//     border-color: #636775;
//     background-color: #636775;
//   }
//   :checked::before {
//     left: 10px;
//   }
//   ::before {
//     content: "";
//     left: 0;
//     transition: left 0.4s;
//     width: 10px;
//     height: 10px;
//     position: absolute;
//     margin-top: 2px;
//     margin-left: 2px;
//     border-radius: 20px;
//     background-color: #ffffff;
//   }
// `;


import { ReactComponent as jsIcon } from "./icons/icon-Js-colors.svg";
//JsIcon
const JsIcon = styled(jsIcon)`
  height: 20px;
  width: 20px;
  float: right;
  margin-right: 16px;
  margin-top: 3px;
`;
<JsIcon /> 
```





```
import { Menu, Dropdown } from "antd";
import React, { useState } from "react";
import styled from "styled-components";
import { ReactComponent as Packup } from "./icons/icon-Pack-up.svg";

//表格内的小型下拉框
const MyDropdown: any = styled(Dropdown)`
  display: inline-block;
  height: 32px !important;
  width: ${(props: any) => (props.width ? props.width : "120px")} !important;
  background: #fdfdfd;
  border: 1.07px solid #d7d9e0;
  border-radius: 6px;
  margin-left: ${(props: any) => props.marginleft || 0};
  margin-top: 7px;
  margin-bottom: 10.5px;
  margin-right: 8px;
`;
//下拉框的小图标
const PackupIcon: any = styled(Packup)`
  height: 20px;
  width: 20px;
  float: right;
  margin-top: 6px;
  margin-bottom: 6px;
  margin-right: 8px;
  color: #8b8fa3;
  transform: rotate(180deg);
  :hover {
    cursor: pointer;
  }
`;
//下拉框的文字
const DropdownLabel = styled.span`
  line-height: 32px;
  font-size: 13px;
  font-family: PingFangSC-Regular;
  color: #333333;
  margin-left: 12px;
`;

interface IConfig {
  label: string;
  key: string;
  onChange?: (val: string) => void;
}
interface IDropDown {
  DropConfig: Array<IConfig>;
  label: string;
  width?: string;
  marginleft?: string;
}

//下拉菜单默认的值
const value = (props: any) => {
  return (
    <Menu>
      {props.map((item: any, index: number) => {
        return (
          <React.Fragment key={index}>
            <Menu.Item
              onClick={() => {
                item.onChange(item.key);
              }}
              key={item.key}
            >
              <p>{item.label}</p>
            </Menu.Item>
          </React.Fragment>
        );
      })}
    </Menu>
  );
};

export const DropDown = (props: IDropDown) => {
  const { label, width, marginleft, DropConfig } = props;
  return (
    <>
      <MyDropdown
        width={width}
        marginleft={marginleft}
        overlay={value(DropConfig)}
        trigger={["click"]}
      >
        <a onClick={(e) => e.preventDefault()}>
          <DropdownLabel>{label}</DropdownLabel>
          <PackupIcon />
        </a>
      </MyDropdown>
    </>
  );
};


```

//表格内的小型下拉框

const MyDropdownn: any = styled(Dropdown)`

  display: inline-block;

  height: 32px !important;

  width: 184px;

  background: #fdfdfd;

  border: 1.07px solid #d7d9e0;

  border-radius: 4px;

  position: absolute;

  left: 112px;

`;

const PackupIcon: any = styled(Packup)`

  height: 20px;

  width: 20px;

  float: right;

  margin-top: 6px;

  margin-bottom: 6px;

  margin-right: 8px;

  transform: rotate(180deg);

  :hover {

    cursor: pointer;

  }

`;

//下拉框的文字

const MyDropdownText = styled.p`

  display: inline-block;

  line-height: 32px;

  font-size: 13px;

  font-family: PingFangSC-Regular;

  color: #333333;

  margin-left: 12px;

  margin-bottom: 0;

`;

//下拉菜单默认的值

const menu = (

  <Menu>

    <Menu.Item key="0">
    
      <p>1st menu item</p>
    
    </Menu.Item>
    
    <Menu.Item key="1">
    
      <p>2st menu item</p>
    
    </Menu.Item>
    
    <Menu.Divider />
    
    <Menu.Item key="3">3rd menu item</Menu.Item>

  </Menu>

);

export const SuspensionBoxDropDown = () => {

  return (

    <>
    
      <MyDropdown overlay={menu} trigger={["click"]}>
    
        <a onClick={(e) => e.preventDefault()}>
    
          <MyDropdownText>{"自动"}</MyDropdownText>
    
          <PackupIcon />
    
        </a>
    
      </MyDropdown>
    
    </>

  );

};





```tsx
import { OnlySwitch } from "./index";

const Switch = styled(OnlySwitch)``;
//switch开关的div
const SwitchDiv = styled.div`
  margin-top: -4.5px;
  margin-bottom: 6.5px;
`;
//switch开关的文字
const SwitchLabel = styled.span`
  display: inline-block;
  font-family: PingFangSC-Regular;
  font-size: 13px;
  color: #222222;
  line-height: 13px;
  margin-left: 8px;
  :hover {
    cursor: default;
  }
`;

export const PackupSwitch = (props: any) => {
  const { label, value, onChange } = props;
  const handlechange = () => {
    console.log(0);
  };
  return (
    <>
      <SwitchDiv>
        <Switch type="checkbox" onClick={handlechange} checked={true}></Switch>
        <SwitchLabel>{label}</SwitchLabel>
      </SwitchDiv>
    </>
  );
};

```