

##### copy

```
const copyText = !isExecutionSuccess(props.query)
? props.query?.children.message.getView()
: _.isObjectLike(data)
? JSON.stringify(data)
: data;

{showCopy && copyText && (
<Affix target={() => resultPanelRef.current}>
<CopyTextButton text={copyText} />
</Affix>
)}
const Affix = styled(AntdAffix)`
  position: absolute;
  right: 16px;
  z-index: 999;
  height: 24px;
  width: 24px;
  background: #ffffffa6;
  border: 1px solid #e1e3eb;
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  & svg g {
    fill: #8b8fa3;
  }
  &:hover {
    background: #ffffff;
  }
  &:hover svg g {
    fill: #315efb;
  }
  .ant-affix {
    top: 0;
  }
`;
```



##### image

```tsx
<div
  style={{
    backgroundImage: `url(${props.src.value})`,
      width: "100%",
      height: "100%",
      backgroundPosition: "center center",
      backgroundRepeat: "no-repeat",
      backgroundSize: "contain",
      display: "flex",
  }}
  ></div>

<Image
  preview={false}
  width={"10%"}
  height={"10%"}
  placeholder={"正在加载"}
  src="https://raw.githubusercontent.com/kongsa-0721/IMG/master/img/cat.JPG"
  fallback="https://raw.githubusercontent.com/kongsa-0721/IMG/master/img/cat.JPG"
/>
```

##### extensions

```tsx
const textStyle = {
  "font-family": "PingFangSC-Regular",
  "word-wrap": "break-word",
  "word-break": "break-all",
  "white-space": "pre-wrap",
  "font-size": "13px",
  "line-height": "13px",
};
const defaultTheme = EditorView.baseTheme({
  "&.cm-editor": {
    backgroundColor: "#ffffff",
    // border: "1px solid #D7D9E0",
    "border-radius": "4px",
    width: "100%",
    "margin-top": "3.5px",
    "font-size": "13px",
    maxHeight: "128px",
    transition: "all .4s ease",
    outline: "none",
    "min-height": "30px",
    overflow: "hidden",
  },
  "& .cm-line": {
    "padding-left": "8px",
    "min-height": "22px",
  },
  ".cm-scroller": {
    "line-height": "22px",
  },
  "&.cm-editor:hover": {
    // border: "1px solid #8B8FA3",
    transition: "all .4s ease",
    outline: "none",
  },
  "&.cm-editor.cm-focused": {
    // border: "1px solid #3377FF",
    // "box-shadow": "0 0 0 2px #d6e4ff",
    outline: "none",
  },
  //大的tooltip
  "&.cm-editor>.cm-tooltip.cm-tooltip-autocomplete": {
    width: "280px",
    padding: "16px 4px 16px 8px",
    backgroundColor: "#FFFFFF",
    "border-radius": "8px",
    "box-shadow": "0 0 10px 0 rgba(0,0,0,0.10)",
    border: "none",
    transform: "translate(-16px, 10px)",
  },
  //左侧的小tooltip
  "&.cm-editor>.cm-tooltip>.cm-completionInfo": {
    "max-width": "280px",
    top: "0px !important",
    right: "calc(100% + 4px)",
    backgroundColor: "#FFFFFF",
    "border-radius": "8px",
    "box-shadow": "0 0 10px 0 rgba(0,0,0,0.10)",
    border: "none",
    padding: "16px",
  },
  //tooltip在右侧时候与大tooltip的间距
  "& .cm-completionInfo.cm-completionInfo-right": {
    left: "calc(100% + 4px)",
  },
  "&.cm-editor>.cm-tooltip>.cm-completionInfo .hintDiv": {
    display: "inline-flex",
    "align-items": "baseline",
    cursor: "pointer",
    color: "#4965F2",
    ...textStyle,
    "font-family": "PingFangSC-Semibold",
  },
  "&.cm-editor>.cm-tooltip>.cm-completionInfo .hintDiv:hover": {
    color: "#3377ff",
  },
  "&.cm-editor>.cm-tooltip>.cm-completionInfo .hintDiv:hover .hintSvg g": {
    stroke: "#3377ff",
  },
  "&.cm-editor>.cm-tooltip>.cm-completionInfo .hintName": {
    display: "inline-block",
    "margin-bottom": "4px",
  },
  "&.cm-editor>.cm-tooltip>.cm-completionInfo .hintType": {
    color: "#8b8fa3",
    display: "block",
    margin: "8px 0px",
    ...textStyle,
  },
  "&.cm-editor>.cm-tooltip>.cm-completionInfo .hintDoc": {
    color: "#333333",
    ...textStyle,
  },
  "&.cm-editor>.cm-tooltip>.cm-completionInfo .hintSvg": {
    transform: "translateY(3px)",
    cursor: "pointer",
  },
  //设置下拉框的最小宽度
  "&.cm-editor>.cm-tooltip.cm-tooltip-autocomplete>ul": {
    "min-width": "200px",
    "scrollbar-gutter": "stable",
  },
  //每一行 li 的style
  "&.cm-editor>.cm-tooltip.cm-tooltip-autocomplete>ul>li": {
    ...textStyle,
    color: "#333333",
    padding: "6px 0px",
    display: "flex",
    "align-items": "center",
  },
  //每一行的鼠标悬停
  "&.cm-editor>.cm-tooltip.cm-tooltip-autocomplete>ul>li:hover": {
    backgroundColor: "rgba(242,247,252,0.7)",
    "border-radius": "4px",
  },
  //每一行的类型文字 function 等等
  "&.cm-editor>.cm-tooltip.cm-tooltip-autocomplete>ul>li>span:last-child": {
    marginLeft: "auto",
    "font-style": "normal",
    color: "#8B8FA3",
    "padding-right": "8px",
    ...textStyle,
  },
  //被选中的行
  "&.cm-editor>.cm-tooltip.cm-tooltip-autocomplete>ul>li[aria-selected]": {
    backgroundColor: "#F2F7FC",
    "border-radius": "4px",
    "font-family": "PingFangSC-Semibold",
  },
  //匹配上的括号
  "&.cm-editor .cm-scroller .cm-content[contenteditable=true] .cm-line .cm-matchingBracket":
    {
      color: "#40A072",
      backgroundColor: "#e9f4e6",
    },
  //滚动条
  "&.cm-editor>.cm-tooltip.cm-tooltip-autocomplete>ul::-webkit-scrollbar": {
    width: "4px",
  },
  "&.cm-editor>.cm-tooltip.cm-tooltip-autocomplete>ul::-webkit-scrollbar-thumb":
    {
      "background-clip": " content-box",
      "border-radius": " 9999px",
      "background-color": "rgba(139, 143, 163, 0.12)",
      height: "50px",
      "min-height": "50px",
      "max-height": "50px",
    },
  "&.cm-editor>.cm-tooltip.cm-tooltip-autocomplete>ul::-webkit-scrollbar-thumb:hover":
    {
      "background-color": "rgba(139, 143, 163, 0.36)",
    },
  //line number
  "& .cm-gutters": {
    width: "40px",
    "background-color": "#f5f5f6",
    color: "#333333",
    "border-radius": "4px 0px 0px 4px",
  },
  "& .cm-lineNumbers .cm-gutterElement": {
    "min-width": "27px",
  },
});
```



##### form(key,val)

```tsx
// interface KeyValueFormItemProps {
//   name: string;
//   label: string;
// }
// export const KeyValueFormItem = (props: KeyValueFormItemProps) => {
//   return (
//     <Form.List
//       name={props.name}
//       initialValue={[{ key: "k1", value: "k2" }] /*todo rm*/}
//     >
//       {(fields, { add, remove }, { errors }) => (
//         <>
//           {fields.map((field, index) => (
//             <Form.Item
//               label={index === 0 ? props.label : ""}
//               key={field.key}
//               wrapperCol={index === 0 ? undefined : { offset: 8, span: 16 }}
//             >
//               <Form.Item
//                 {...field}
//                 name={[field.name, "key"]}
//                 key={field.key + "key"}
//                 noStyle
//               >
//                 <Input placeholder="key" style={{ width: "30%" }} />
//               </Form.Item>
//               <Form.Item
//                 {...field}
//                 name={[field.name, "value"]}
//                 key={field.key + "value"}
//                 noStyle
//               >
//                 <Input placeholder="value" style={{ width: "50%" }} />
//               </Form.Item>
//               {fields.length > 1 ? (
//                 <MinusCircleOutlined
//                   className="dynamic-delete-button"
//                   onClick={() => remove(field.name)}
//                 />
//               ) : null}
//             </Form.Item>
//           ))}
//           <Form.Item wrapperCol={{ offset: 8, span: 16 }}>
//             <Button
//               type="dashed"
//               onClick={() => add()}
//               style={{ width: "60%" }}
//               icon={<PlusOutlined />}
//             >
//               添加
//             </Button>
//             <Form.ErrorList errors={errors} />
//           </Form.Item>
//         </>
//       )}
//     </Form.List>
//   );
// };
```



##### collapse

```tsx
info: (completion: Completion) => {
  function createNode(txt: string) {
    const template = `<div class='child'>${txt}</div>`;
    let tempNode = document.createElement("div");
    tempNode.innerHTML = template;
    return tempNode.firstChild;
  }
  return document.getElementById("as")?.appendChild(createNode(completion.detail as string) as Node) as Node;
},


collapse
import { Collapse as AntdCollapse } from "antd";
import { ReactComponent as Packup } from "./icons/icon-unfold.svg";
import styled from "styled-components";
import React, { ReactElement } from "react";
const { Panel } = AntdCollapse;

const Contain = styled.div`
  background: #f2f7fc;
  //每一项的padding 每一个展开项 这里可以设置margin
  .ant-collapse > .ant-collapse-item > .ant-collapse-header {
    padding: 0;
    height: 25px;
    margin-left: 2px;
    font-family: PingFangSC-Medium;
    font-size: 13px;
    color: #333333;
    line-height: 25px;
    user-select: none;
  }
  //每一项的内容的边距 字体
  .ant-collapse-ghost
    > .ant-collapse-item
    > .ant-collapse-content
    > .ant-collapse-content-box {
    padding: 0;
    font-family: PingFangSC-Medium;
    font-size: 13px;
    line-height: 13px;
    margin-left: 14px;
    user-select: none;
  }
  //图标的边距
  .ant-collapse
    > .ant-collapse-item
    > .ant-collapse-header
    .ant-collapse-arrow {
    margin-right: 0;
  }
`;
const PackupIcon = styled(Packup)<{ deg?: number }>`
  height: 12px;
  width: 12px;
  margin-right: 2px;
  transform: rotate(${(props) => (props.deg ? props.deg : 0)}deg);
`;
interface IpanelConfig {
  title: string;
  data: JSX.Element | ReactElement | string;
  key: number | string;
}
interface Iprops {
  Config: Array<IpanelConfig>;
  activeKey: string | string[];
  changeKey: (config: string | string[]) => void;
}
export const Collapse = (props: Iprops) => {
  const { Config, activeKey, changeKey } = props;
  const handlechange = (e: string | string[]) => {
    console.log(e);
    changeKey && changeKey(e);
  };
  return (
    <Contain>
      <AntdCollapse
        activeKey={activeKey}
        ghost
        expandIcon={({ isActive }) =>
          isActive ? <PackupIcon /> : <PackupIcon deg={-90} />
        }
        onChange={handlechange}
      >
        {Config && Config.length > 0
          ? Config.map((item, index) => {
              return (
                <React.Fragment key={index}>
                  <Panel header={item.title} key={item.key} showArrow={true}>
                    {item.data}
                  </Panel>
                </React.Fragment>
              );
            })
          : null}
      </AntdCollapse>
    </Contain>
  );
};
import { useState } from "react";
import { CompName, Section } from ".";
import { Collapse } from "./Collapase";

const Content = () => {
  const [key, setkey] = useState<string | string[]>([]);
  const handlechange = (config: string | string[]) => {
    setkey(config);
  };
  return (
    <Collapse
      activeKey={key}
      changeKey={handlechange}
      Config={[
        { title: "first", data: "data1", key: 1 },
        { title: "Second", data: "data2", key: 2 },
        { title: "Third", data: "data3", key: 3 },
      ]}
    />
  );
};
export const LeftContent = () => {
  const [key, setkey] = useState<string | string[]>([]);
  const handlechange = (config: string | string[]) => {
    setkey(config);
  };
  return (
    <>
      <CompName compname="Page 01" width={288} />
      <Section name="搜索需求与变量" width={288}>
        <Collapse
          activeKey={key}
          changeKey={handlechange}
          Config={[
            { title: "first", data: <Content />, key: 1 },
            { title: "Second", data: "data2", key: 2 },
            { title: "Third", data: "data3", key: 3 },
          ]}
        />
      </Section>
    </>
  );
};

```

##### app


```tsx
import CompAction from "comps/actions";
import { Comp } from "comps/baseComps";
import { OptionalComp } from "comps/baseComps/comp";
import { evalAndReduce } from "comps/utils";
import { Tabs, Bottom, BottomLeft, BottomRight } from "design";
import Header, { Panelstatus } from "pages/common/header";
import React from "react";
import { GlobalStateManager } from "state";
import { GridLayoutComp, COMP_TYPES } from "./comps/comps/gridLayoutComp";
import { CompContext } from "./comps/utils";
import {
  Body,
  Height100Div,
  LeftPanel,
  MiddlePanel,
  EditorContainer,
  BottomContainer,
  RightPanel,
  Resize,
} from "./pages";
import LeftPanelContainer from "./pages/leftPanel/leftPanelContainer";

const VariantComp = GridLayoutComp;
class App extends React.Component {
  state: {
    comp: Comp;
    showPropertyPane: boolean;
    selectedPath?: Array<string>;
    isDragging: boolean;
    showPanels: Panelstatus;
  };
  constructor(props: any) {
    super(props);
    this.dispatch = this.dispatch.bind(this);
    const compStr = localStorage.getItem("main_comp");
    let comp = new VariantComp({ dispatch: this.dispatch });
    if (compStr) {
      const defaultValue = JSON.parse(compStr);
      comp = new VariantComp({ dispatch: this.dispatch, value: defaultValue });
    }
    // console.log("after " + JSON.stringify(comp));
    console.log(comp.toJsonValue());
    comp = evalAndReduce(comp);
    GlobalStateManager.DEFAULT.setComp(comp);
    this.state = {
      comp: comp,
      showPropertyPane: false,
      isDragging: false,
      showPanels: { left: true, middle: true, right: true },
    };
    this.showLeft = this.showLeft.bind(this);
    this.showMiddle = this.showMiddle.bind(this);
    this.showRight = this.showRight.bind(this);
    this.resizeBottom = this.resizeBottom.bind(this);
  }
  drag = React.createRef<HTMLDivElement>();
  bottom = React.createRef<HTMLDivElement>();
  resizeBottom(e: any) {
    var startY = e.clientY;
    var _this = this;
    const curH = (_this.bottom.current as HTMLDivElement).clientHeight;
    e.stopPropagation();
    e.preventDefault();
    document.onmousemove = function (e) {
      e.stopPropagation();
      e.preventDefault();
      var endY = e.clientY;
      var moveLen = startY - endY;
      (_this.bottom.current as HTMLDivElement).style.height =
        curH + moveLen + "px";
    };
    document.onmouseup = () => {
      document.onmousemove = null;
      document.onmouseup = null;
    };
  }
  dispatch(action: CompAction) {
    if (action.type === "SELECT_COMP") {
      // console.log("select " + JSON.stringify(action));
      this.setState((state) => {
        const comp = this.state.comp;
        const selectedComp = comp.selectChild(action.path);
        const hasComp = selectedComp && selectedComp !== comp;
        return { selectedPath: action.path, showPropertyPane: hasComp };
      });
      return;
    }
    console.log("receive action " + JSON.stringify(action));
    this.setState((state: { comp: Comp }) => {
      let comp = state.comp.reduce(action);
      // console.log(comp.getView());
      // console.log("after action " + JSON.stringify(action) + ", " + JSON.stringify(comp));
      comp = evalAndReduce(comp);
      // 这儿是插入中间件的地方，可能的功能有:
      // Evaluation、Persistence、UndoRedo、MultiUser
      GlobalStateManager.DEFAULT.setComp(comp);
      localStorage.setItem("main_comp", JSON.stringify(comp.toJsonValue()));
      return { comp: comp };
    });
  }
  showLeft() {
    this.setState((state: { showPanels: Panelstatus }) => {
      let status = { ...state.showPanels, left: !state.showPanels.left };
      return { showPanels: status };
    });
  }
  showMiddle() {
    this.setState((state: { showPanels: Panelstatus }) => {
      let status = { ...state.showPanels, middle: !state.showPanels.middle };
      return { showPanels: status };
    });
  }
  showRight() {
    this.setState((state: { showPanels: Panelstatus }) => {
      let status = { ...state.showPanels, right: !state.showPanels.right };
      return { showPanels: status };
    });
  }
  render() {
    const comp = this.state.comp;
    let selectedComp: OptionalComp;
    if (this.state.selectedPath) {
      selectedComp = comp.selectChild(this.state.selectedPath);
      if (selectedComp === comp) {
        // hack，之后用更好的方式表达反选
        selectedComp = undefined;
      }
    }
    const setDragging = (dragging: boolean) => {
      this.setState({ isDragging: dragging });
    };
    const showPropertyPane = this.state.showPropertyPane;
    function rightPanelView() {
      if (showPropertyPane) {
        if (selectedComp) {
          return selectedComp.getPropertyView();
        } else {
          return <div>没有组件被选择，点击选择组件</div>;
        }
      }
      return COMP_TYPES.map((compType) => (
        <div
          draggable
          onDragStart={(e) => {
            e.dataTransfer.setData("compType", compType);
            setDragging(true);
          }}
        >
          {compType}
        </div>
      ));
    }
    return (
      <Height100Div
        onDragEnd={(e) => {
          setDragging(false);
        }}
      >
        <Header
          setShow={[this.showLeft, this.showMiddle, this.showRight]}
          curStatus={this.state.showPanels}
        />
        <Body>
          <LeftPanel show={this.state.showPanels.left}>
            <LeftPanelContainer />
          </LeftPanel>
          <MiddlePanel>
            <EditorContainer>
              <CompContext.Provider
                value={{
                  selectedComp: selectedComp,
                  isDragging: this.state.isDragging,
                  setDragging: setDragging,
                }}
              >
                {comp.getView()}
              </CompContext.Provider>
            </EditorContainer>
            <Resize onMouseDown={this.resizeBottom} ref={this.drag} />
            <BottomContainer
              show={this.state.showPanels.middle}
              ref={this.bottom}
            >
              <Bottom>
                <BottomLeft />
                <BottomRight />
              </Bottom>
            </BottomContainer>
          </MiddlePanel>
          <RightPanel show={this.state.showPanels.right}>
            <Tabs
              onChange={(key) =>
                this.setState({ showPropertyPane: key === "property" })
              }
              tabsConfig={[
                { key: "property", title: "属性" },
                { key: "insert", title: "插入" },
              ]}
              activeKey={this.state.showPropertyPane ? "property" : "insert"}
            >
              {rightPanelView()}
            </Tabs>
          </RightPanel>
        </Body>
      </Height100Div>
    );
  }
}

export default App;

```

##### app内容

```tsx
import CompAction from "comps/actions";
import { EditorContext, EditorState } from "comps/editorState";
import { evalAndReduce } from "comps/utils";
import { Tabs, Bottom, BottomLeft, BottomRight, Insert, Search, noComp } from "design";
import Header from "pages/common/header";
import React, { useContext } from "react";
import { GlobalStateManager } from "state";
import { useUserViewMode } from "util/hooks";
import { COMP_TYPES, GridLayoutComp } from "./comps/comps/gridLayoutComp";
import {
  Body,
  Height100Div,
  LeftPanel,
  MiddlePanel,
  EditorContainer,
  BottomContainer,
  RightPanel,
} from "./pages";
import LeftPanelContainer from "./pages/leftPanel/leftPanelContainer";

class App extends React.Component {
  state: {
    editor: EditorState;
  };
  constructor(props: any) {
    super(props);
    this.dispatch = this.dispatch.bind(this);
    const compStr = localStorage.getItem("main_comp");
    let comp = new GridLayoutComp({ dispatch: this.dispatch });
    if (compStr) {
      const defaultValue = JSON.parse(compStr);
      comp = new GridLayoutComp({
        dispatch: this.dispatch,
        value: defaultValue,
      });
    }
    console.log(comp.toJsonValue());
    comp = evalAndReduce(comp);
    GlobalStateManager.DEFAULT.setComp(comp);
    const editorState = new EditorState(comp, (changeEditorStateFn) => {
      this.setState((state: { editor: EditorState }) => {
        return { editor: changeEditorStateFn(state.editor) };
      });
    });
    this.state = { editor: editorState };
    this.resizeBottom = this.resizeBottom.bind(this);
  }
  //bottom上下拖动
  drag = React.createRef<HTMLDivElement>();
  bottom = React.createRef<HTMLDivElement>();
  resizeBottom(e: any) {
    var startY = e.clientY;
    var _this = this;
    const curH = (_this.bottom.current as HTMLDivElement).clientHeight;
    e.stopPropagation();
    e.preventDefault();
    document.onmousemove = function (e) {
      e.stopPropagation();
      e.preventDefault();
      var endY = e.clientY;
      var moveLen = startY - endY;
      (_this.bottom.current as HTMLDivElement).style.height = curH + moveLen + "px";
    };
    document.onmouseup = () => {
      document.onmousemove = null;
      document.onmouseup = null;
    };
  }
  dispatch(action: CompAction) {
    const editorState = this.state.editor;
    console.log("receive action " + JSON.stringify(action));
    editorState.setComp((oldComp) => {
      // console.log("oldComp " + JSON.stringify(oldComp.toJsonValue()));
      const comp = evalAndReduce(oldComp.reduce(action));
      // console.log("newComp " + JSON.stringify(comp.toJsonValue()));
      // 这儿是插入中间件的地方，可能的功能有:
      // Evaluation、Persistence、UndoRedo、MultiUser
      GlobalStateManager.DEFAULT.setComp(comp);
      localStorage.setItem("main_comp", JSON.stringify(comp.toJsonValue()));
      return comp;
    });
  }

  render() {
    const editorState = this.state.editor;
    const comp = editorState.comp;
    let selectedComp = editorState.selectedComp();
    const setDragging = (dragging: boolean) => {
      editorState.setDragging(dragging);
    };
    const showPropertyPane = editorState.showPropertyPane;
    function rightPanelView() {
      if (showPropertyPane) {
        if (selectedComp) {
          return selectedComp.getPropertyView();
        } else {
          return noComp;
          // <div>没有组件被选择，点击选择组件</div>
        }
      }
      return (
        <>
          <Search value="" onChange={() => {}} />
          <Insert
            COMP_TYPES={COMP_TYPES}
            onDrag={() => {
              setDragging(true);
            }}
          />
        </>
      );
    }
    return (
      <EditorContext.Provider value={editorState}>
        <Height100Div
          onDragEnd={(e) => {
            setDragging(false);
          }}
        >
          <Header
            changePanelStatus={(ps) => editorState.setPanelStatus(ps)}
            panelStatus={editorState.panelStatus}
            previewFn={() => {}}
          />
          <Body>
            {editorState.panelStatus.left && (
              <LeftPanel>
                <LeftPanelContainer />
              </LeftPanel>
            )}
            <MiddlePanel>
              <EditorContainer>{comp.getView()}</EditorContainer>
              <Resize onMouseDown={this.resizeBottom} ref={this.drag} />
              {editorState.panelStatus.bottom && (
                <BottomContainer ref={this.bottom}>
                  <Bottom>
                    <BottomLeft />
                    <BottomRight />
                  </Bottom>
                </BottomContainer>
              )}
            </MiddlePanel>
            {editorState.panelStatus.right && (
              <RightPanel>
                <Tabs
                  onChange={(key) => editorState.setShowPropertyPane(key === "property")}
                  tabsConfig={[
                    { key: "property", title: "属性" },
                    { key: "insert", title: "插入" },
                  ]}
                  activeKey={editorState.showPropertyPane ? "property" : "insert"}
                >
                  {rightPanelView()}
                </Tabs>
              </RightPanel>
            )}
          </Body>
        </Height100Div>
    return (
      <EditorContext.Provider value={this.state.editor}>
        <AppView />
      </EditorContext.Provider>
    );
  }
}

/**
 * 单独提出来就可以用hooks了
 */
function AppView() {
  const isUserViewMode = useUserViewMode();
  const editorState = useContext(EditorContext);
  const comp = editorState.comp;
  const selectedComp = editorState.selectedComp();
  const showPropertyPane = editorState.showPropertyPane;
  function rightPanelView() {
    if (showPropertyPane) {
      if (selectedComp) {
        return selectedComp.getPropertyView();
      } else {
        return <div>没有组件被选择，点击选择组件</div>;
      }
    }
    return COMP_TYPES.map((compType) => (
      <div
        draggable
        onDragStart={(e) => {
          e.dataTransfer.setData("compType", compType);
          editorState.setDragging(true);
        }}
      >
        {compType}
      </div>
    ));
  }
  if (isUserViewMode) {
    return comp.getView();
  }
  return (
    <Height100Div
      onDragEnd={(e) => {
        editorState.setDragging(false);
      }}
    >
      <Header
        changePanelStatus={(ps) => editorState.setPanelStatus(ps)}
        panelStatus={editorState.panelStatus}
      />
      <Body>
        {editorState.panelStatus.left && (
          <LeftPanel>
            <LeftPanelContainer />
          </LeftPanel>
        )}
        <MiddlePanel>
          <EditorContainer>{comp.getView()}</EditorContainer>
          {editorState.panelStatus.bottom && (
            <BottomContainer>
              <Bottom>
                <BottomLeft />
                <BottomRight />
              </Bottom>
            </BottomContainer>
          )}
        </MiddlePanel>
        {editorState.panelStatus.right && (
          <RightPanel>
            <Tabs
              onChange={(key) =>
                editorState.setShowPropertyPane(key === "property")
              }
              tabsConfig={[
                { key: "property", title: "属性" },
                { key: "insert", title: "插入" },
              ]}
              activeKey={editorState.showPropertyPane ? "property" : "insert"}
            >
              {rightPanelView()}
            </Tabs>
          </RightPanel>
        )}
      </Body>
    </Height100Div>
  );
}

export default App;

```

##### 拖动

```tsx
  const contain = useRef<HTMLDivElement>(null);
  const left = useRef<HTMLDivElement>(null);
  const drag = useRef<HTMLDivElement>(null);
  const right = useRef<HTMLDivElement>(null);
  //拖动的函数
  const Move = (e: any) => {
    var startX = e.clientX;
    const width = (contain.current as HTMLDivElement).offsetWidth;
    var lefttt = (drag.current as HTMLDivElement).offsetLeft;
    document.onmousemove = function (e) {
      var endX = e.clientX;
      var moveLen = lefttt + (endX - startX);
      var maxT = (contain.current as HTMLDivElement).clientWidth - (drag.current as HTMLDivElement).offsetWidth;
      if (moveLen < width / 2.25) moveLen = width / 2.25;
      if (moveLen > maxT - width / 2.25) {
        moveLen = maxT - width / 2.25;
      }
      (drag.current as any).style.left = moveLen;
      (left.current as HTMLDivElement).style.width = moveLen + "px";
      (right.current as HTMLDivElement).style.width =
        (contain.current as HTMLDivElement).clientWidth - moveLen - 1 + "px";
    };
    document.onmouseup = function () {
      document.onmousemove = null;
      document.onmouseup = null;
      (drag.current as any).releaseCapture && (drag.current as any).releaseCapture();
    };
    return false;
  };
  
  
    cursor: ew-resize;
  ::before {
    content: "";
    position: absolute;
    height: 284px;
    width: 5px;
    left: -4px;
    cursor: ew-resize;
  }
  ::after {
    content: "";
    position: absolute;
    height: 284px;
    width: 5px;
    cursor: ew-resize;
  }
  
  //抽离出来的hooks
  export const useColor = (initial: string) => {
  const [Color, setColor] = useState<string>(initial);
  const ChangeC: (item: string) => void = (item: string) => {
    setColor(item);
  };
  return [Color, ChangeC] as const;
};
const [Color, ChangeC] = useColor("");
ChangeC(keys.length ? "#F2F7FC" : "");
```

##### dropdown

```tsx
import { Radio, Select } from "antd";
import { SimpleComp } from "comps/baseComps";
import { TextLabel, BranchStyle } from "design";
import { ReactComponent as Packup } from "design/icons/icon-Pack-up.svg";
import * as React from "react";
import { ReactNode } from "react";
import { ControlParams } from "./controlParams";
import styled, { css } from "styled-components";
import { changeValueAction } from "comps/actions";

const { Option } = Select;

export const DropdownIcon = styled(Packup)`
  margin-bottom: 2px;
  margin-right: 2px;
`;
export const DropdownContainer = styled.div<{ proportion?: number }>`
  display: inline-block !important;
  height: 32px;
  caret-color: transparent;
  flex-basis: ${(props) => props.proportion}%;
  max-width: calc(${(props) => props.proportion}% - 4px);
  flex-grow: 0;

  .ant-select-arrow {
    top: 65%;
    right: 8px;
    transform: rotate(180deg);
  }

  /*默认状态 */

  .ant-select-item {
    background: #efeff1;
    background-color: #ffffff;
  }

  .ant-select:not(.ant-select-customize-input) .ant-select-selector {
    background: #ffffff;
    border: 1.07px solid #d7d9e0;
    border-radius: 4px;
  }

  /*hover态的边框颜色 */

  .ant-select:not(.ant-select-customize-input) .ant-select-selector:hover {
    border-color: #8b8fa3;
  }

  /*focus态的边框以及背景 */

  .ant-select-focused:not(.ant-select-disabled).ant-select:not(.ant-select-customize-input) .ant-select-selector {
    background: #ffffff;
    border-color: #3377ff !important;
    box-shadow: 0 0 0 2px rgba(51, 119, 255, 0.2);
  }

  /**设置一个长度 不会出现溢出现象 */

  .ant-select-single .ant-select-selector .ant-select-selection-item {
    width: 32px;
  }

  /**预置文案的颜色 */

  .ant-select-single .ant-select-selector .ant-select-selection-placeholder {
    color: #b8b9bf;
  }

  /**展开时图标旋转并改变颜色 */
  .ant-select.ant-select-open .ant-select-arrow {
    transform: rotate(0deg) !important;
    top: 12px;
    right: 15px;
  }
  .ant-select.ant-select-open .ant-select-arrow ${DropdownIcon} path {
    fill: #3377ff;
  }
`;

const RadioButtonContainer = styled.div<{ width: number }>`
  border-radius: 6px;
  padding: 2px;
  background-color: #efeff1;
  //大框架的背景色和字体颜色
  .ant-radio-button-wrapper {
    font-family: PingFangSC-Medium;
    background-color: #efeff1;
    font-size: 12px;
    color: #8b8fa3;
    border: none;
    height: 24px;
    line-height: 24px;
    width: ${(props) => props.width}px;
    text-align: center;
  }

  //被点击的item的圆角
  .ant-radio-button-wrapper-checked {
    border-radius: 4px;
  }

  //被点击的背景色 字体颜色
  .ant-radio-button-wrapper-checked:not(.ant-radio-button-wrapper-disabled) {
    font-family: PingFangSC-Medium;
    color: #333333;
    background-color: #ffffff;
    border: none;
    font-weight: bold;
  }

  //伪类 中间的一条蓝色的竖线
  .ant-radio-button-wrapper-checked:not(.ant-radio-button-wrapper-disabled)::before {
    background-color: transparent !important;
  }

  .ant-radio-button-wrapper-checked:not(.ant-radio-button-wrapper-disabled):focus-within {
    border-radius: 4px;
  }

  //改变shadow 不然会有一个蓝色的shadow
  .ant-radio-button-wrapper:focus-within {
    box-shadow: none;
  }

  .ant-radio-button-wrapper:not(:first-child)::before {
    background-color: transparent;
  }

  /*hover态 */

  .ant-radio-button-wrapper:hover {
    color: #222222;
    border-radius: 4px;
  }
`;
const FlexDiv = styled.div<{ $type: string }>`
  display: flex;
  justify-content: space-between;
  padding-bottom: 4px;
  padding-top: 4px;
  width: 100%;
  padding-right: 16px;
  ${(props) => {
    switch (props.$type) {
      case "tree":
        return css`
          position: relative;
          flex-direction: column;
          align-items: flex-start;
          gap: 8px;
          ${BranchStyle};
        `;
      default:
        return css`
          flex-direction: row;
          align-items: center;
        `;
    }
  }}
`;
type OptionType = {
  readonly label: ReactNode;
  readonly value: string;
};
export type OptionsType = readonly OptionType[];
export type ValueFromOption<Options extends OptionsType> = Options[number]["value"];

export function Dropdown<T extends OptionsType>(props: {
  label: string;
  options: T;
  defaultValue: ValueFromOption<T>;
  onChange: (value: ValueFromOption<T>) => void;
  radioButton?: boolean;
  type?: "tree" | "normal";
}) {
  return (
    <FlexDiv $type={props.type ? props.type : "normal"}>
      <TextLabel label={props.label} />
      {!props.radioButton && (
        <DropdownContainer>
          <Select
            defaultValue={props.defaultValue}
            style={{ width: props.type === "tree" ? 256 : 160 }}
            onChange={(x) => props.onChange(x)}
            suffixIcon={<DropdownIcon />}
          >
            {props.options.map((item, index) => {
              return (
                <Option key={item.value} value={item.value}>
                  {item.label}
                </Option>
              );
            })}
          </Select>
        </DropdownContainer>
      )}
      {props.radioButton && (
        <RadioButtonContainer width={props.type !== "tree" ? 180 / props.options.length : 252 / props.options.length}>
          <Radio.Group onChange={(e) => props.onChange(e.target.value)} defaultValue={props.defaultValue}>
            {props.options.map((item, index) => {
              return (
                <Radio.Button key={item.value} value={item.value}>
                  {item.label}
                </Radio.Button>
              );
            })}
          </Radio.Group>
        </RadioButtonContainer>
      )}
    </FlexDiv>
  );
}

export function dropdownControl<T extends OptionsType>(options: T, defaultValue: ValueFromOption<T>) {
  class DropdownControl extends SimpleComp<ValueFromOption<T>> {
    override getDefaultValue(): ValueFromOption<T> {
      return defaultValue;
    }

    propertyView(params: ControlParams & { radioButton?: boolean }): ReactNode {
      return (
        <Dropdown
          defaultValue={this.value}
          options={options}
          radioButton={params.radioButton}
          label={params.label}
          onChange={(value) => this.dispatch(changeValueAction(value))}
        />
      );
    }

    getPropertyView(): ReactNode {
      throw new Error("Method not implemented.");
    }
  }

  return DropdownControl;
}

export function OnlyDropdown<T extends OptionsType>(props: {
  options: T;
  defaultValue?: ValueFromOption<T>;
  onChange: (value: ValueFromOption<T>) => void;
  proportion?: number;
  placeholder?: string;
  dropdownRender?: (menu: React.ReactElement) => React.ReactElement;
}) {
  return (
    <DropdownContainer proportion={props.proportion}>
      <Select
        defaultValue={props.defaultValue}
        style={{
          width: "100%",
          minWidth: "120px",
          maxWidth: "100%",
        }}
        maxTagCount={"responsive" as const}
        onChange={(x) => props.onChange(x)}
        suffixIcon={<DropdownIcon />}
        dropdownMatchSelectWidth={false}
        placeholder={props.placeholder}
        dropdownRender={props.dropdownRender}
      >
        {props.options.map((item, index) => {
          return (
            <Option key={item.value} value={item.value}>
              {item.label}
            </Option>
          );
        })}
      </Select>
    </DropdownContainer>
  );
}

```

##### login

```vue
<template>
  <div id="login">
    <span>
      <router-link to="/">主页面</router-link>
    </span>
    <div class="container">
      <link
        href="https://cdn.jsdelivr.net/npm/@bootcss/v3.bootcss.com@1.0.4/dist/css/bootstrap.min.css"
        rel="stylesheet"
      />
      <form class="form-signin">
        <h2 class="form-signin-heading">请登陆</h2>
        <label for="inputEmail" class="sr-only">Account</label>
        <input
          type="email"
          v-model="account"
          id="inputEmail"
          class="form-control"
          placeholder="Account"
          required
          autofocus
          autocomplete="off"
        />
        <label for="inputPassword" class="sr-only">Password</label>
        <input
          type="password"
          v-model="password"
          id="inputPassword"
          class="form-control"
          placeholder="Password"
          required
        />
        <div class="checkbox">
          <label>
            <input type="checkbox" value="remember-me" /> Remember me
          </label>
        </div>
        <button class="btn btn-lg btn-primary btn-block" type="submit" @click="login">Sign in</button>
      </form>
      <!-- <div>{{this.$router.currentRoute.path}}</div> 这是当前的路由路径 -->
    </div>
    <!-- /container -->
  </div>
</template>

<script>
export default {
  name: "login",
  data() {
    return {
      ins: [],
      account: "",
      password: "",
    };
  },
  methods: {
    login() {
      this.$axios({
        method: "POST",
        url: "/user/login",
        data: {
          username: this.account,
          password: this.password,
          lossTime: 100,
        },
        headers: {
          "X-Rust-AuthToken": "",
          "X-Rust-RequestId": "",
        },
      })
        // this.$axios.get('/Rust/test')
        .then((response) => {
          if (response.data) {
            this.ins = response.data.data;
            this.$message({
              showClose: true,
              message: "登录成功",
              type: "success",
            });
            var mytoken = JSON.stringify(response.headers["x-rust-authtoken"]);
            var myrequestid = JSON.stringify(
              response.headers["x-rust-requestid"]
            );
            sessionStorage.setItem("token", mytoken);
            sessionStorage.setItem("requestid", myrequestid);
            // localStorage.setItem()
            //通过则跳转
            // this.$router.push({path : '/'})
          }
        })
        .catch((err) => {
          if (err.response.data.code == 403) {
            this.$message({
              showClose: true,
              message: err.response.data.msg,
              type: "error",
            });
          }
        });
    },
  },
};
</script>

<style scoped>
.container {
  padding-top: 150px;
}
.btn-primary {
  background-color: rgb(245, 178, 189);
}
.btn-primary:hover {
  background-color: rgb(199, 39, 65);
}
body {
  padding-top: 40px;
  padding-bottom: 40px;
  background-color: #eee;
}

.form-signin {
  max-width: 330px;
  padding: 15px;
  margin: 0 auto;
}
.form-signin .form-signin-heading,
.form-signin .checkbox {
  margin-bottom: 10px;
}
.form-signin .checkbox {
  font-weight: 400;
}
.form-signin .form-control {
  position: relative;
  -webkit-box-sizing: border-box;
  -moz-box-sizing: border-box;
  box-sizing: border-box;
  height: auto;
  padding: 10px;
  font-size: 16px;
}
.form-signin .form-control:focus {
  z-index: 2;
}
.form-signin input[type="text"] {
  margin-bottom: -1px;
  border-bottom-right-radius: 0;
  border-bottom-left-radius: 0;
}
.form-signin input[type="password"] {
  margin-bottom: 10px;
  border-top-left-radius: 0;
  border-top-right-radius: 0;
}
.form-control {
  border-radius: 18px 18px 18px 18px;
}
#inputPassword {
  border-radius: 18px 18px 18px 18px;
}
</style>>


```





```
<template>
  <div id="login">
    <span>
      <router-link to="/">主页面</router-link>
    </span>
    <div id="container">
      <h2>请登录</h2>
      <el-form>
        <el-form-item>
          <input
            v-model="account"
            type="text"
            class="form-control"
            placeholder="  Account"
            required
            autofocus
            autocomplete="off"
          />
        </el-form-item>
        <el-form-item>
          <input
            v-model="password"
            type="password"
            id="inputPassword"
            class="form-control"
            placeholder="  Password"
            required
          />
        </el-form-item>
        <button class="btn-primary" type="submit" @click="login">Sign in</button>
      </el-form>
    </div>
    <!-- /container -->
  </div>
</template>

<script>
export default {
  name: "login",
  data() {
    return {
      ins: [],
      account: "",
      password: "",
    };
  },
  methods: {
    login() {
      this.$axios({
        method: "POST",
        url: "/user/login",
        data: {
          username: this.account,
          password: this.password,
          lossTime: 100,
        },
        headers: {
          "X-Rust-AuthToken": "",
          "X-Rust-RequestId": "",
        },
      })
        // this.$axios.get('/Rust/test')
        .then((response) => {
          if (response.data) {
            this.ins = response.data.data;
            this.$message({
              showClose: true,
              message: "登录成功",
              type: "success",
            });
            var mytoken = JSON.stringify(response.headers["x-rust-authtoken"]);
            var myrequestid = JSON.stringify(
              response.headers["x-rust-requestid"]
            );
            sessionStorage.setItem("token", mytoken);
            sessionStorage.setItem("requestid", myrequestid);
            // localStorage.setItem()
            //通过则跳转
            // this.$router.push({path : '/'})
          }
        })
        .catch((err) => {
          if (err.response.data.code == 403) {
            this.$message({
              showClose: true,
              message: err.response.data.msg,
              type: "error",
            });
          }
        });
    },
  },
};
</script>

<style scoped>
#newlogin {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen,
    Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
}
#container {
  margin: auto;
  margin-top: 200px;
  width: 400px;
}
input {
  margin: -1px;
  border: 1px solid #ccc;
  font-size: 16px;
  height: 44px;
  width: 300px;
  border-radius: 18px 18px 18px 18px;
  font-size: 16px;
}
h2 {
  font-size: 30px;
}
.btn-primary {
  height: 46px;
  width: 300px;
  background-color: rgb(245, 178, 189);
  border: 1px solid rgb(22, 22, 22);
  border-radius: 6px;
  font-size: 16px;
  color: aliceblue;
}
.btn-primary:hover {
  background-color: rgb(199, 39, 65);
}
</style>


```





```
   //使用三目运算符来控制是否展示 实际上是控制虚拟Dom的展示
   //
   {/* {showContent ? (
        <>
          <RightCompNameFn compname="Table1"></RightCompNameFn>
          <RightFlodPanelFn flodname="基础">
            <RightFlodPanelBasic></RightFlodPanelBasic>
          </RightFlodPanelFn>
          <RightFlodPanelFn flodname="表格">
            <RightPanelForm></RightPanelForm>
          </RightFlodPanelFn>
        </>
      ) : (
        <>Insert</>
      )} */}
```
