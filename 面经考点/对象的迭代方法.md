## JS中对象的迭代方法

##### 利用 for in 可以遍历对象

for 是最基本的这个我们就略过了  for...in是ES5的标准，该方法遍历的是对象的属性名称(key：键名)。一个Array对象也是一个对象，数组中的每个元素的索引被视为属性名称，所以在使用for...in遍历Array时，拿到的是每个元素索引
一般用于遍历对象自身的和继承的可枚举属性。以及对象从构造函数原型中继承的属性。对于每个不同的属性，语句都会被执行。
不建议使用for in 遍历数组，因为输出的顺序是不固定的。
如果迭代的对象的变量值是null或者undefined, for in不执行循环体，建议在使用for in循环之前，先检查该对象的值是不是null或者undefined
for…in 语句以原始插入顺序迭代对象的可枚举属性(只能迭代出可枚举的属性，可枚举属性【js自定义属性】/不可枚举属性【对象的内置属性，如数组的length 就是一个内置属性，所以for…in遍历不出来】)。

for in的原理是: Object.keys()  ：返回给定对象所有可枚举属性的字符串数组.  //这个在后面有提到

```jsx
//这里的i是每一项的属性名 obj[i]是键值
var obj={a:1, b:2};
for(let i in obj){
   console.log(i);  // a  b  
   console.log(obj[i]);  //  1  2
}
//这个 i 是每一项的下标 
var arr = [1,2,3,4,5,6,7]
for(let i in arr){
  console.log(i);  
}
//for in 不可以用来循环遍历Set集合
//for in 不可以用来遍历MAP
```

##### for of

for...of是ES6的标准，该方法遍历的是对象的属性所对应的值(value：键值)。所以它用来遍历数组时得到每个元素的值
for…of 语句在可迭代对象（包括 Array，Map，Set，String，TypedArray，arguments 对象等等）上创建一个迭代循环，调用自定义迭代钩子，并为每个不同属性的值执行语句
for…of 语句遍历可迭代对象定义要迭代的数据（非自定义属性）
for…of循环可以使用的范围包括数组、Set 和 Map 结构、某些类似数组的对象、Generator 对象，以及字符串。

**for of 遍历数组得到的是每一个值 for in 得到的是每一个元素的下标**

```js
//遍历对象直接报错  TypeError: obj is not iterable  不可以遍历普通的对象
//给对象添加一个属性 [Symbol.iterator]: Array.prototype[Symbol.iterator] 就可以遍历了 底层是根据这个判断的
var obj={a:1, b:2};
for(let of in obj){
   console.log(i);    
   console.log(obj[i]);
} 
var s = new Set(['A','B','C']);
var m = new Map([[1:'x'],[2:'y'],[3,'z']]);
//for of 遍历输出set的是Set集合每个元素的值 A　B　C
// for of 遍历map 既可以拿到键名，也可以拿到键值，输出的是值 A　B　C
```

##### Object.keys()/value()的使用

`Object.keys` 返回一个所有元素为字符串的数组，其元素来自于从给定的`object`上面可直接枚举的属性。这些属性的顺序与手动遍历该对象属性时的一致。

Object.keys在内部会根据属性名key的类型进行不同的排序逻辑。分三种情况：
	如果属性名的类型是Number，那么Object.keys返回值是按照key从小到大排序
	如果属性名的类型是String，那么Object.keys返回值是按照属性被创建的时间升序排序。
	如果属性名的类型是Symbol，那么逻辑同String相同
原来当Object.keys函数使用参数时，会执行以下步骤：
	第一步：将参数转换成Object类型的对象。
	第二步：通过转换后的对象获得属性列表properties。
		注意：属性列表properties为List类型（List类型是ECMAScript规范类型）
	第三步：将List类型的属性列表properties转换为Array得到最终的结果。
		将参数转换成Object（ToObject(O)）

```js
var anObj = {
    100: 'a', 2: 'b', 7: 'c', 99999: "最大值", 0: "最小值"
};
var ress = Object.keys(anObj)
console.log(ress);
var res = Object.values(anObj)
console.log(res);

for (let i of ress) {
    console.log(i);
}
for (let i of res) {
    console.log(i);
}
[ '0', '2', '7', '100', '99999' ]
[ '最小值', 'b', 'c', 'a', '最大值' ]
```

###### 与 for in 的区别

```js
//object对象没有length属性，可以通过Object.keys(person).length，来获取person的长度了。 不会遍历原型链
Object.prototype.say="cgl";
var person ={ age: 18};
console.log(Object.keys(person));//结果  ["age"]
//使用 for in 循环遍历对象的属性时，原型链上的所有属性都将被访问
for (let i in person) {
    console.log(i);   // age say
}
//只遍历对象自身的属性，而不遍历继承于原型链上的属性，使用hasOwnProperty 方法过滤
for (let i in person) { 
    if(person.hasOwnProperty(i)){
        console.log(i);
    }
}

//使用for of 也可以遍历了 但是要结合 这个函数使用
for(var key of Object.keys(person)){
    //使用Object.keys()方法获取对象key的数组
    console.log(person[key]);  //18
    console.log(key);     //age
}
```

##### forEach
forEach() 方法用于调用数组的每个元素，并将元素传递给回调函数。该方法没有返回值。
**注意:**
1、 forEach() 对于空数组是不会执行回调函数的。
2、 for可以用continue跳过循环中的一个迭代，forEach用continue会报错。
3、 forEach() 需要用 return 跳过循环中的一个迭代，跳过之后会执行下一个迭代。

回调函数需要三个参数  *array.forEach(function(currentValue, index, arr), thisValue)*

| 参数           | 描述                                                         |
| :------------- | :----------------------------------------------------------- |
| *currentValue* | 必需。当前元素                                               |
| *index*        | 可选。当前元素的索引值。                                     |
| *arr*          | 可选。当前元素所属的数组对象。                               |
| *thisValue*    | 可选。传递给函数的值一般用this值，如果为空，"undefined"会传递给"this"值。 |

```js
let colors = ['red', 'blue', 'green'];
colors.forEach((item, index, arr) => {
  console.log(`${item} || ${index} || ${arr}`);
})
//输出值
red || 0 || red,blue,green
blue || 1 || red,blue,green
green || 2 || red,blue,green
```

没有办法终止或跳出forEach循环，除非抛出一个异常。

如果需要终止或者跳出循环，建议用some()或者every()。

##### every()

every()方法测试一个数组内的所有元素是否都能通过某个指定函数的测试。它返回一个布尔值。

**注意**：若收到一个空数组，此方法在一切情况下都会返回 true。

​			不会改变原数组，不会对空数组进行检测

​			如果数组中检测到有一个元素不满足，则整个表达式返回 *false* ，且剩余的元素不会再进行检测。

​			如果所有元素都满足条件，则返回 true。

```js
array.every(function(currentValue,index,arr), thisValue)
function isBigEnough(element, index, array) {
  return element >= 10;
}
[12, 5, 8, 130, 44].every(isBigEnough);   // false
[12, 54, 18, 130, 44].every(isBigEnough); // true
```

##### some()

some() 方法测试数组中是不是至少有1个元素通过了被提供的函数测试。它返回的是一个Boolean类型的值。

如果传一个空数组 返回值默认为false

```js
arr.some(callback(element[, index[, array]])[, thisArg])
var fruits = ['apple', 'banana', 'mango', 'guava'];

function checkAvailability(arr, val) {
  return arr.some(function(arrVal) {
    return val === arrVal;
  });
}

checkAvailability(fruits, 'kela');   // false
checkAvailability(fruits, 'banana'); // true
```

与every使用方法大体一致

##### filter()

filter()方法创建一个新数组, 其包含通过所提供函数实现的测试的所有元素。 

返回值：一个新的、由通过测试的元素组成的数组，如果没有任何数组元素通过测试，则返回空数组。

```js
var newArray = arr.filter(callback(element[, index[, array]])[, thisArg])
var arr = [12, 5, 8, 130, 44];
function isBigEnough(element) {
	return element >= 10;
}
var filtered = arr.filter(isBigEnough);
console.log(filtered);//[ 12, 130, 44 ]
```

##### map()

map()方法创建一个新数组，其结果是该数组中的每个元素是调用一次提供的函数后的返回值。

返回值是一个新的数组 并不会改变原来的数组

```js
var new_array = arr.map(function callback(currentValue[, index[, array]]) {
 // Return element for new_array 
}[, thisArg])
var kvArray = [{key: 1, value: 10},
               {key: 2, value: 20},
               {key: 3, value: 30}];

var reformattedArray = kvArray.map(function(obj) {
   var rObj = {};
   rObj[obj.key] = obj.value;
   return rObj;
});

// reformattedArray 数组为： [{1: 10}, {2: 20}, {3: 30}],
```

##### JS中数组的哪些方法改变了原数组

###### 不改变原数组

- concat 连接两个或者更多的数组 并返回结果 

- join 把数组的所有元素放入一个字符串  

- slice 从已有数组中返回选定的元素 

- toString 将数组转化为字符串 并返回结果

- **forEach不确定** Map every some filter reduce

- 不对原数组产生影响,foreach不会产生新数组

  3,map因为返回数组所以可以链式操作，foreach不能

  4, map里可以用return ,而foreach里用return不起作用，foreach不能用break，会直接报错

###### 改变原数组

- pop 删除数组的最后一个元素 
- push 向数组的最后一个位置添加一个元素 
- reverse 颠倒数组中各个元素的位置 
- shift 删除 数组中第一个元素的值 并返回该元素 
- unshift 向数组的开头添加一个或者多个元素 并返回新的长度
- sort 对数组元素进行排序 从小到大
- splice 向数组中添加 删除元素 返回被删除的元素 
  - splice(4,1) //从第四个位置删除一个元素
  - splice(1, 0, "yellow", "orange");  //从位置1开始插入两项
  - splice(1, 1, "red", "purple");  //插入两项，删除一项

##### 迭代器

关于迭代器的好文章https://github.com/mqyqingfeng/Blog/issues/90



数组的reduce方法  两个参数 第一个参数是一个函数 有四个参数。第二个参数是初始值

- Accumulator：MDN上解释为累计器，但我觉得不恰当，按我的理解它应该是截至当前元素，之前所有的数组元素被reducer函数处理累计的结果
- Current：当前被执行的数组元素
- CurrentIndex: 当前被执行的数组元素索引
- SourceArray：原数组，也就是调用reduce方法的数组

```js
const arr = [1, 2, 3, 4]
const accumulator = (total, current, currentIndex, arr) => {
  console.log(total, current, currentIndex, arr);
  return total + current
}
console.log(arr.reduce(accumulator,3))
```

