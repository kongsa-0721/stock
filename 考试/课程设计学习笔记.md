# 课程设计学习笔记

使用jdbc技术对数据库进行操作

```java
import java.sql.*;

public class JDBCdemo {
    //mysql驱动包名
    private static final String DRIVER_NAME = "com.mysql.cj.jdbc.Driver";
    //数据库连接地址
    private static final String URL = "jdbc:mysql://localhost:3306/mysql?servetTimezone=UTC";
    //用户名
    private static final String USER_NAME = "root";
    //密码
    private static final String PASSWORD = "123456";

    public static void main(String[] args) {
        //获取mysql数据库的驱动
        try {
            Class.forName(DRIVER_NAME);
            //获取数据库连接对象
            Connection conn = DriverManager.getConnection(URL,USER_NAME,PASSWORD);
            //创建sql执行对象；mysql查询语句
            String sql = "SELECT * FROM job.work_info";
            //执行sql命令，返回结果集
            PreparedStatement prst = conn.prepareStatement(sql);
            //处理结果集，依次关闭结果
            ResultSet rs = prst.executeQuery();
            while (rs.next()) {
                System.out.println("用户名:" + rs.getString("id"));
                System.out.println("密码:" + rs.getString("name"));
            }
            //关闭流 先开的后关闭
            rs.close();
            prst.close();
            conn.close();
        } catch (ClassNotFoundException | SQLException e) {
            e.printStackTrace();
        }
    }
}
```

### 数据库连接池

可以在同一时间进行多个线程对数据库进行访问

常用的有 druid c3p0

1.导入相应的jar包

2.定义配置文件

3.加载配置文件

4.获取连接池对象

5.获取连接对象

```sql
/usr/local/MySQL/bin/mysql -u root -p

create database staffIfo;
create table teacherInfo;
(id int (4) not null auto_increment comment '编号',
num int (10) not null comment '教工号',
name varchar (20) not null comment '姓名',
sex varchar (4) not null default '男' comment '性别',
birthday date null comment '出生日期',
address varchar (50) null comment '家庭住址',
primary key (id))
engine = innodb default charset = gbk collate = gbk_bin;

//设置字段唯一
ALTER TABLE teacherInfo ADD unique(id);
ALTER TABLE teacherInfo ADD unique(num);
//更改字段数据类型
ALTER TABLE teacherInfo MODIFY name varchar (30);
//更改字段位置顺序
ALTER TABLE teacherInfo MODIFY sex varchar(4) AFTER birthday;
//更改字段名字
ALTER TABLE teacherInfo CHANGE num t_id int (10);
//删除字段
ALTER TABLE teacherInfo DROP address;
//添加字段
ALTER TABLE teacherInfo ADD wages FLOAT;
//更改表名
ALTER TABLE teacherInfo rename as teacherInfoInfo;
```



```sql
/usr/local/MySQL/bin/mysql -u root -p
create database staffIfo;
create table department 
(d_id int (4) not null comment '部门号',
d_name varchar (20) not null comment '部门名',
functio varchar (50) null comment '部门职能',
address varchar (20) null comment '部门位置',
primary key (d_id))
ALTER TABLE department ADD unique(d_id);
ALTER TABLE department ADD unique(d_name);

create table worker(
id int(4) not null unique comment '编号',
num int(10) not null unique comment '员工号',
d_id int(4) null comment'部门号',
name varchar(20) not null comment '姓名',
sex varchar(4) not null comment '性别',
birthday date null comment '出生日期',
address varchar(50) null comment'家庭住址',
primary key(id))
//添加外键
alter table worker add  foreign key(d_id) references department(d_id);
//删除外键
ALTER TABLE worker_kongsa drop foreign key worker_kongsa_ibfk_1;
//对表重新命名
ALTER TABLE worker rename as worker_kongsa;
```



1.2

```sql
create table teacherInfo
(num int (10) not null comment '教工号',
name varchar (20) not null comment '姓名',
sex varchar (4) not null comment '性别',
birthday DATETIME null comment '出生日期',
address varchar (50) null comment '家庭住址',
primary key (num))
ALTER TABLE teacherInfo ADD unique(num);
INSERT INTO teacherInfo
VALUES (1001,'张龙','男','1984-11-08','北京市昌平区');
INSERT INTO teacherInfo
VALUES (1002,'李梅','女' ,'1970-01-21','北京市海淀区');
INSERT INTO teacherInfo
VALUES (1003,'王丰','男' ,'1976-10-30','北京市昌平区');
INSERT INTO teacherInfo
VALUES (1004,'赵六','男' ,'1980-06-05 ','北京市顺义区');
UPDATE teacherInfo SET birthday='2002-11-08' WHERE num=1003;
UPDATE teacherInfo SET address='北京市朝阳区' WHERE sex='男';
DELETE FROM teacherInfo where num =1002;
```

1.3

```sql
create database test3_kongsa;
use test3_kongsa;
//创建表 bumen_kongsa
create table bumen_kongsa
(d_id int (4) not null comment '部门号',
c_name varchar (20) not null comment '部门名称',
c_function varchar (20) null comment '部门职能',
address varchar (20) null comment '工作地点',
primary key (d_id));
ALTER TABLE bumen_kongsa ADD unique(d_id);
ALTER TABLE bumen_kongsa ADD unique(c_name);
//创建表yuangong_kongsa
create table yuangong_kongsa
(id int (4) not null comment '员工号',
name varchar (20) not null comment '姓名',
sex varchar (4) not null comment '性别',
birthday DATE null comment '出生日期',
d_id int (4) not null comment '部门号',
salary Float null comment '工资',
address varchar (50) null comment '家庭住址',
primary key (d_id));
//唯一
ALTER TABLE yuangong_kongsa ADD unique(id);
//设置外键
alter table yuangong_kongsa add foreign key(d_id) references bumen_kongsa(d_id);
//插入数据
INSERT INTO bumen_kongsa VALUES 
(1001,'人事部','人事管理','北京'),
(1002,'科研部','研发产品','北京'),
(1003,'生产部','产品生产','天津'),
(1004,'销售部','产品销售','上海');
//插入数据
INSERT INTO yuangong_kongsa VALUES 
(8001,'韩鹏','男','25',1002,4000,'北京市海淀区'),
(8002,'张峰','男','26' ,1001,2500,'北京市昌平区'),
(8003,'欧阳','男' ,'20',1003, 1500,'湖南省永州市'),
(8004,'王武','男' ,'30',1001 ,3500,'北京市顺义区'),
(8005,'欧阳宝贝','女' ,'21' ,1002 ,3000,'北京市昌平区'),
(8006,'呼延' ,'男' ,'28' , 1003 , 1800,'天津市南开区');
select * from yuangong_kongsa;
select * from yuangong_kongsa where id>=8004 and id<=8005;
select d_id,c_name,c_function from bumen_kongsa; 
select d_id from bumen_kongsa where c_name='人事部'or c_name='科研部';
select * from yuangong_kongsa where d_id = 1001 or d_id =1002;
select * from yuangong_kongsa where birthday>=25 and birthday <=30;
//查询家是北京的
SELECT * from yuangong_kongsa where address like '北京%';
//排序
SELECT d_id,COUNT(d_id)
from yuangong_kongsa
GROUP BY d_id
HAVING count(d_id)>=0;
//查最高工资
select d_id,max(salary)
from yuangong_kongsa
group by d_id;
//左链接
SELECT *
from bumen_kongsa left join yuangong_kongsa on bumen_kongsa.d_id=yuangong_kongsa.d_id;
//计算总工资
select d_id,avg(salary)
from yuangong_kongsa
group by d_id;
//由低到高排序
select * from yuangong_kongsa order by salary desc;
```

2.1

```sql
create database job;
use job;
create table userlogin_kongsa(
	id int(4) not null unique primary key auto_increment,
	name varchar(20) not null,
	password varchar(20) not null,
	info text
);
create table information_kongsa(
	id int(4) not null primary key unique auto_increment,
	name varchar(20) not null,
	sex varchar(4) not null,
	birthday date,
	address varchar(50),
	tel varchar(20),
	pic blob
);
desc userlogin;
desc information;
create index index_name on information_kongsa (name(10));
create index index_bir on information_kongsa (birthday,address);
alter table information_kongsa add index index_id(id ASC);
create index index_userlogin_kongsa on userlogin_kongsa(id(4));
drop index index_userlogin_kongsa on userlogin_kongsa;
show create table userlogin_kongsa \G
drop index index_name on information_kongsa;

create table work_info(
	id int(4) primary key not null unique,
	name varchar(20) not null,
	sex varchar(4) not null,
	age int(4),
	address varchar(50),
	tel varchar(20)
);
desc work_info;

insert into work_info(id,name,sex,age,address,tel)
values
(1,'张明','男',19,'北京市朝阳区','1234567'),
(2,'李广','男',21,'北京市昌平区','2345678'),
(3,'王丹','女',18,'湖南省永州市','3456789'),
(4,'赵一枚','女',24,'浙江省宁波市','4567890');

create algorithm=merge view info_view(id,name,sex,address) as 
select id,name,sex,address from work_info where age>20 with local check option;
desc info_view;
select * from info_view;
alter algorithm=merge view info_view(id,name,sex,address) as 
select id,name,sex,address from work_info where age<20 with local check option;
update info_view set sex='女' where id=3;
drop view info_view;
```

3.1

```sql
create database foodinfo;

use foodinfo;

create table food(
foodid int(4) primary key not null unique auto_increment,
name varchar(20) not null,
company varchar(30) not null,
price float not null,
product_time year,
validity_time int(4),
address varchar(50)
);

desc food;


//机房
insert into food(name,company,price,product_time,validity_time,address)
values
('QQbingban','QQbingbanchang',2.5,'2008',3,'Beijing'),
('MNniunai','MNniunaichang',3.5,'2009',1,'Hebei'),
('EEguodong','EEguodongchang',1.5,'2007',2,'Beijing'),
('FFkafei','FFkafeichang',20,'2002',5,'Tianjin'),
('GGnaitang','GGnaitangchang',14,'2003',3,'Guangdong');

select * from food;

insert into food(name,company,price,product_time,validity_time,address)
values
('QQ饼干','QQ饼干厂',2.5,'2008',3,'北京'),
('MN牛奶','MN牛奶厂',3.5,'2009',1,'河北'),
('EE果冻','EE果冻厂',1.5,'2007',2,'北京'),
('FF咖啡','FF咖啡厂',20,'2002',5,'天津'),
('GG奶糖','GG奶糖厂',14,'2003',3,'广东');

select * from food;

delimiter &&
create procedure pfood_price_count(in price_info1 float,in price_info2 float,out count int)
reads sql data
begin
declare temp float;
declare match_price cursor for select price from food;
declare exit handler for not found close match_price;
set @sum=0;
select count(*) into count from food
where price>price_info1 and price<price_info2;
open match_price;
repeat
fetch match_price into temp;
if temp>price_info1 and temp<price_info2
then set @sum=@sum+temp;
end if;
until 0 end repeat;
close match_price;
end &&
delimiter ;
call pfood_price_count(2,18,@count); 
select @count,@sum;
drop procedure pfood_price_count; 

delimiter &&
create function pfood_price_count1(price_info1 float,price_info2 float)
returns int reads sql data
begin
return(select count(*) from food
where price>price_info1 and price<price_info2);
end &&
delimiter ;

select pfood_price_count1(2,18);
drop function pfood_price_count1;

```



```sql
create table bumen_sheet(
bumen_num int primary key,
bumen_name varchar(20) unique not null,
bumen_renshu int(10) not null
);


create table yuangong_sheet(
	number int not null primary key,
	name varchar(20) not null,
	age int CHECK(age>=1) not null,
	sex varchar(2) CHECK(sex in('男','女')) not null,
	birthday Datetime not null,
	martial varchar(4) CHECK(martial in ('已婚','未婚')),
	political varchar(20) CHECK(political in ('群众','团员','党员')) not null,
	schoollevel varchar(20)not null,
	bumen_num int nut null ,
	position varchar(20) not null,
	contact varchar(11) unique not null,
	note varchar(100),
	foreign key (bumen_num) references bumen_sheet(bumen_num)
)

create table salary_sheet(
salary_num int primary key,
number int not null,
basic_salary int CHECK(basic_salary>0) not null,
welfare int CHECK(welfare>0) not null,
bonus int CHECK(bonus>0) not null,
calculation_method varchar(50) not null,
real_wages int CHECK(real_wages>0),
issuing_date Datetime not null,
foreign key (number) references yuangong_sheet(number)
)

create table rd_sheet(
	rd_num int primary key,
  number int not null,
  rd_time Datetime not null,
  rd_why varchar(100) not null,
  rd_position varchar(20) not null,
  note varchar(500),
  foreign key (number) references yuangong_sheet(number)
)
```



```sql
create table bumen_sheet(
bumen_num int primary key,
bumen_name varchar(20) unique not null,
bumen_renshu int(10) not null
);


create table yuangong_sheet(
	number int not null primary key,
	name varchar(20) not null,
	age int CHECK(age>=1) not null,
	sex varchar(2) CHECK(sex in('男','女')) not null,
	birthday Datetime not null,
	martial varchar(4) CHECK(martial in ('已婚','未婚')),
	political varchar(20) CHECK(political in ('群众','团员','党员')) not null,
	schoollevel varchar(20)not null,
	bumen_num int nut null ,
	position varchar(20) not null,
	contact varchar(11) unique not null,
	note varchar(100),
	foreign key (bumen_num) references bumen_sheet(bumen_num)
)

create table salary_sheet(
salary_num int primary key,
number int not null,
basic_salary int CHECK(basic_salary>0) not null,
welfare int CHECK(welfare>0) not null,
bonus int CHECK(bonus>0) not null,
calculation_method varchar(50) not null,
real_wages int CHECK(real_wages>0),
issuing_date Datetime not null,
foreign key (number) references yuangong_sheet(number)
)

create table rd_sheet(
	rd_num int primary key,
  number int not null,
  rd_time Datetime not null,
  rd_why varchar(100) not null,
  rd_position varchar(20) not null,
  note varchar(500),
  foreign key (number) references yuangong_sheet(number)
)
```

